# Database config
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/casadocodigo

# JPA/Hibernate config
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.integrator_provider=br.com.zup.casadocodigo.infra.jpa.ClassImportIntegratorIntegratorProvider

## Schema Generation - Uncomment to generate DDL at startup
# spring.jpa.properties.javax.persistence.schema-generation.scripts.action=update
# spring.jpa.properties.javax.persistence.schema-generation.scripts.create-target=ddl.sql

# API
server.servlet.context-path=/api

# Flyway
logging.level.org.flywaydb=debug